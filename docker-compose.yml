# Infra local: PostgreSQL para o Prisma (backend).
# Uso: pnpm docker:up  →  configure backend/.env  →  pnpm db:setup

services:
  postgres:
    image: postgres:16-alpine
    container_name: msedit-xml-db
    restart: unless-stopped
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-msedit}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-msedit}
      POSTGRES_DB: ${POSTGRES_DB:-msedit_xml}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB",
        ]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

volumes:
  postgres_data:
    name: msedit-xml-postgres-data
