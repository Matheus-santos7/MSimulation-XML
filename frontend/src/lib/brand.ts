/** Identidade visual e nomenclatura do produto. */
export const BRAND = {
  /** Letra destacada do wordmark (Matheus Santos). */
  namePrefix: "M",
  /** Restante do nome em mono — lê-se junto: MSimulation. */
  nameStem: "Simulation",
  /** Nome principal completo. */
  name: "MSimulation",
  /** Sufixo técnico — destaca o foco em XML fiscal. */
  suffix: "XML",
  /** Slug técnico (npm, Docker, repositório). */
  slug: "msimulation-xml",
  /** Nome completo para metadata e títulos. */
  fullName: "MSimulation XML",
  /** Descrição curta da função do produto. */
  tagline: "Fluxo logístico e fiscal fulfullment",
  /** Subtítulo para login e onboarding. */
  description:
    "Simule, NF-e, Eventos fiscais e CT-e e monte cenários para operações fulfullment.",
  author: "Matheus Santos",
} as const;
